
$(document).ready( function () {

    /// Mobile menu ////////////////////////
    $('.menu-link').bigSlide();
    $('#lightback').height($(document).height());
    $('#lightback').width($(document).width());
    ////////////////////////////////////////////////
    
});
